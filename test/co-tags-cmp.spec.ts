import 'reflect-metadata'
import 'zone.js'
import { CoTagsCmp } from '../src/co-tags/co-tags-cmp'

describe('CoTagsCmp', function() {
  it('can add a new tag', function() {
    expect(CoTagsCmp).not.toBe(undefined)
  })
})
